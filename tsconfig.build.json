{
  "extends": "./tsconfig.json", // ベース設定をextendsで読み込み
  "compilerOptions": {
    "outDir": "./dist" // 出力先をdistに設定
  },
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts", "prisma", "vercel"] // 除外するディレクトリ
}
